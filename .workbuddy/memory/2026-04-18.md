# 2026-04-18

## Codex Intel Rebuilder 构建任务

### 修复的 bug
- 版本检测正则匹配问题：原脚本的 `detect_codex_version_from_binary_strings` 函数通过正则从二进制扫描版本号，匹配到 `client_id0.122.0-alpha.1PoisonError...` 字符串，由于正则没有严格边界，把 `PoisonError` 也纳入版本号，导致 npm 找不到不存在的包。
- 修复方案：新增最可靠策略——直接在 `strings` 输出里寻找独立成行、格式为 `0.x.y-xxx` 的版本号字符串。修改 `build-intel.sh` 脚本的 `detect_codex_version_from_binary_strings` 函数。

### 清理工作
- 卸载之前挂载的 DMG 卷（hdiutil detach）
- 终止卡住的 npm 安装进程（PID 90004）

### 当前状态
- 构建进程已重新启动（PID 92968），正在执行 npm 依赖安装阶段（约 47% 进度，预计总时间 20 分钟）
- 版本检测现在能正确提取 `0.122.0-alpha.1`
- 构建最终结果：**成功** ✅
- 输出文件：`Codex-Intel.dmg`（389MB），生成于 00:56
- 全程耗时：约 20 分钟（npm 安装耗时 19 分钟，原生模块重编译约 30 秒）
- 所有关键二进制均验证为 x86_64：Electron、codex CLI、rg、better_sqlite3.node、pty.node